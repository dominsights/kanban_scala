package board.core

import org.junit.Assert.*

class CardSuite extends munit.FunSuite {
  test("should create card with title and description") {
    val title = "title"
    val description = "description"

    
    //val card = Card.newCard(title, description, Non)

  }
}
